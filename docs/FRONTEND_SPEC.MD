# FRONTEND SPECIFICATION — MusicMe

## 1. Общая концепция

Одностраничное приложение (SPA) с тёмной темой, минималистичным дизайном и акцентом на обложки альбомов.

**Атмосфера:** "Современный музыкальный сервис" (Spotify / Apple Music / Яндекс.Музыка), но без лишней сложности.

**Ключевая метафора:** Фасетный поиск — пользователь видит все фильтры одновременно и свободно комбинирует их в любом порядке.

---

## 2. Архитектура фронтенда
src/
├── main.js # Точка входа Vue
├── App.vue # Корневой компонент
├── router/ # Vue Router (если потребуется)
├── stores/ # Pinia stores
│ ├── catalog.js # Жанры, настроения, артисты, альбомы
│ ├── filters.js # Текущие выбранные фильтры
│ └── player.js # Состояние плеера
├── api/ # Слой работы с API
│ └── client.js # Axios инстанс
├── components/
│ ├── layout/
│ │ └── HeaderBar.vue # Поиск + OAuth кнопки
│ ├── filters/
│ │ ├── FilterCarousel.vue # Универсальная карусель
│ │ ├── GenreFilter.vue
│ │ ├── MoodFilter.vue
│ │ ├── ArtistFilter.vue
│ │ └── AlbumFilter.vue
│ ├── results/
│ │ ├── AlbumGrid.vue # Плитка альбомов
│ │ └── AlbumCard.vue # Карточка альбома
│ ├── player/
│ │ └── AudioPlayer.vue # Нижний плеер
│ └── ui/
│ ├── Modal.vue # Модальное окно
│ ├── Spinner.vue # Индикатор загрузки
│ └── FilterChips.vue # Чипы активных фильтров
└── assets/
└── styles/
└── main.css # Tailwind + кастомные стили

text

---

## 3. Макет страницы (Layout)
┌─────────────────────────────────────────────────────────────┐
│ HEADER (логотип + поиск + OAuth кнопки) │
├─────────────────────────────────────────────────────────────┤
│ │
│ [ЖАНР] Горизонтальная карусель (всегда видима) │
│ │
├─────────────────────────────────────────────────────────────┤
│ │
│ [НАСТРОЕНИЕ] Горизонтальная карусель (всегда видима) │
│ │
├─────────────────────────────────────────────────────────────┤
│ │
│ [АРТИСТ] Горизонтальная карусель (всегда видима) │
│ │
├─────────────────────────────────────────────────────────────┤
│ │
│ [АЛЬБОМ] Горизонтальная карусель (всегда видима) │
│ │
├─────────────────────────────────────────────────────────────┤
│ │
│ ЧИПЫ АКТИВНЫХ ФИЛЬТРОВ (если есть) │
│ │
├─────────────────────────────────────────────────────────────┤
│ │
│ РЕЗУЛЬТАТЫ (сетка обложек альбомов / список треков) │
│ │
├─────────────────────────────────────────────────────────────┤
│ ПЛЕЕР (появляется при выборе трека) │
└─────────────────────────────────────────────────────────────┘

text

---

## 4. Компоненты

### 4.1. HeaderBar

**Описание:** Верхняя панель.

**Элементы:**
- Логотип "MusicMe" (слева) и подпись проекта мелким серым текстом
- Строка поиска (по центру)
- Кнопки OAuth (справа): Google, Яндекс, VK

**Поведение:**
- Ввод в строку поиска (мин. 2 символа) запускает глобальный поиск через `GET /api/v1/search/?query=...`
- Поиск активируется с задержкой (debounce 500 мс)
- При клике на любую OAuth-кнопку открывается модальное окно с текстом "Раздел в разработке"
- Кнопки OAuth — стилизованные, выглядят как настоящие (логотип + текст + фирменные цвета)

**Состояния:**
- Пустой поиск — ничего не происходит
- Поиск активен — показывать спиннер в строке поиска
- Результаты поиска — отображаются в секции результатов (п. 4.6)
- Поиск не дал результатов — сообщение "По запросу '...' ничего не найдено"

**Взаимодействие с фильтрами:**
- Поиск работает ПАРАЛЛЕЛЬНО с фильтрами
- Если активен и поиск, и фильтры — результаты объединяются через **AND**
- При вводе поискового запроса фильтры НЕ сбрасываются

---

### 4.2. FilterCarousel (универсальный компонент)

**Описание:** Горизонтальная карусель с прокруткой (scroll-snap).

**Входные параметры (props):**

| Prop | Тип | Обязательный | Описание |
|------|-----|--------------|----------|
| `items` | Array | Да | Список элементов для отображения |
| `selectedId` | Number | Нет | ID выбранного элемента (или null) |
| `title` | String | Да | Название карусели ("Жанр", "Настроение"...) |
| `loading` | Boolean | Нет | Флаг загрузки |
| `emptyText` | String | Нет | Текст при пустом списке |

**События (emits):**

| Событие | Параметр | Описание |
|---------|----------|----------|
| `select` | `item` | Пользователь кликнул по элементу |
| `clear` | — | Пользователь кликнул по кнопке "Очистить" |

**Визуальное поведение:**
- Карточки располагаются горизонтально
- Каждая карточка — скруглённый прямоугольник с текстом
- Выбранная карточка подсвечивается (акцентный цвет + обводка)
- Прокрутка — плавная, через CSS `scroll-snap-type: x mandatory`
- На мобильных — swipe, на десктопе — скролл колёсиком + стрелки (опционально)
- Если есть выбранный элемент — рядом с заголовком появляется кнопка "Очистить" (крестик)

**Состояния:**
- `loading = true` — показывать скелетон (placeholder-карточки)
- `items.length === 0` и `loading = false` — показывать `emptyText`
- `items.length > 0` — обычный режим

---

### 4.3. Специализированные карусели

#### 4.3.1. GenreFilter

| Аспект | Значение |
|--------|----------|
| Источник данных | `GET /api/v1/genres/` (при старте) |
| Заголовок | "Жанр" |
| Тип карточки | Текстовая (название жанра) |
| Поведение | Всегда показывает все жанры |

**Карточка:**
┌─────────────┐
│ Hip-Hop │
└─────────────┘

text

---

#### 4.3.2. MoodFilter

| Аспект | Значение |
|--------|----------|
| Источник данных | `GET /api/v1/moods/` (при старте) |
| Заголовок | "Настроение" |
| Тип карточки | Текстовая (название настроения) |
| Поведение | Всегда показывает все настроения |

**Карточка:**
┌─────────────┐
│ Грустное │
└─────────────┘

text

---

#### 4.3.3. ArtistFilter

| Аспект | Значение |
|--------|----------|
| Источник данных | `GET /api/v1/artists/?genre_id=X&mood_id=Y` |
| Заголовок | "Артист" |
| Тип карточки | Картинка (аватар) + имя |
| Поведение | Обновляется при изменении жанра или настроения |

**Карточка:**
┌─────────────┐
│ ┌─────┐ │
│ │ IMG │ │
│ └─────┘ │
│ Мот │
│ 5 альбомов │
└─────────────┘

text

**Обновление:**
- При выборе жанра → перезапрос с `genre_id`
- При выборе настроения → перезапрос с `mood_id`
- При выборе обоих → перезапрос с обоими параметрами
- При сбросе жанра и настроения → запрос без параметров (все артисты)

**Пустое состояние:**
- "Нет артистов с такими параметрами"

---

#### 4.3.4. AlbumFilter

| Аспект | Значение |
|--------|----------|
| Источник данных | `GET /api/v1/albums/?artist_id=X&genre_id=Y&mood_id=Z` |
| Заголовок | "Альбом" |
| Тип карточки | Обложка (миниатюра) + название + год |
| Поведение | Обновляется при изменении жанра, настроения или артиста |

**Карточка:**
┌─────────────┐
│ ┌─────────┐ │
│ │ COVER │ │
│ └─────────┘ │
│ Лучшие хиты │
│ 2023 │
└─────────────┘

text

**Обновление:**
- При выборе артиста → перезапрос с `artist_id`
- При выборе жанра → перезапрос с `genre_id`
- При выборе настроения → перезапрос с `mood_id`
- При комбинации → перезапрос со всеми параметрами
- При сбросе всех → запрос без параметров (все альбомы)

**Пустое состояние:**
- "Нет альбомов с такими параметрами"

---

### 4.4. FilterChips (Чипы активных фильтров)

**Описание:** Отображение выбранных фильтров над результатами.

**Расположение:** Между каруселями и результатами.

**Пример:**
[Жанр: Hip-Hop ×] [Настроение: Грустное ×] [Артист: Мот ×] [Сбросить все]

text

**Поведение:**
- Клик на крестик в чипе — сброс только этого фильтра
- Клик на "Сбросить все" — сброс всех фильтров
- Если фильтры не выбраны — секция скрыта

---

### 4.5. AlbumGrid (Результаты)

**Описание:** Сетка обложек альбомов или список треков.

**Режимы отображения:**

| Режим | Условие | Содержимое |
|-------|---------|------------|
| Список треков | Нет поиска (фильтры любые или пустые) | Треки, прошедшие **все** выбранные фильтры |
| Результаты поиска | Активен поисковый запрос (и альбом не выбран) | Смешанный результат (артисты, альбомы, треки) |

**Формат списка треков:**
- Без фильтров: все треки каталога по порядку (альбом, затем номер в альбоме), **не более 20**
- Любой фильтр (жанр, настроение, артист, альбом) сужает этот список по AND
- Выбор альбома не показывает «все треки альбома»: остаются только те, что совпадают с жанром/настроением/артистом, если они выбраны
- Строка трека: обложка слева (своя или обложка альбома), название, артист — альбом, теги, длительность, Play
- Если выбран альбом — сверху шапка альбома (обложка, название, описание) и номера треков

Карусели альбомов/артистов по-прежнему фильтруются как фасеты. Нижняя область — всегда треки, не сетка альбомов.

Пустые состояния:

Ситуация	Сообщение
Нет фильтров и нет треков в базе	"Контент скоро появится"
Фильтры не дали треков	"Ничего не найдено. Измените параметры."
Поиск не дал результатов	"По запросу '...' ничего не найдено"

---

### 4.6. AlbumCard

**Пропсы:**

| Prop | Тип | Описание |
|------|-----|----------|
| `album` | Object | Объект альбома из API |

**Визуал:**
- Обложка (если `cover_url` — картинка, иначе placeholder)
- Название альбома (жирный шрифт)
- Имя артиста (вторичный текст)
- Год (вторичный текст)
- Количество треков (бейдж)

**Поведение:**
- Клик → загрузить треки альбома (`GET /api/v1/albums/{id}/`) и отобразить их списком
- Hover → лёгкое увеличение обложки + тень

---

### 4.7. Список треков

**Описание:** Основное содержимое секции результатов.

**Содержимое:**
- Если выбран альбом: шапка (обложка, название, артист, год, описание) и номера треков
- Список: обложка слева, название, артист — альбом, длительность, жанры/настроения (теги)
- Кнопка "Play" напротив каждого трека
- Кнопка "Текст" — только если у трека есть lyrics
- Кнопка "Минус" — только если у трека есть `minus_url`

**Поведение:**
- Клик на трек или "Play" → воспроизведение оригинала; очередь = текущий отфильтрованный список
- Клик на "Минус" → воспроизведение минусовки этого трека (если `minus_url` есть)
- Обложка: `cover_url` трека, иначе `album_cover_url`

---

### 4.8. Результаты поиска

**Описание:** Отображается при активном поисковом запросе.

**Секции (если есть результаты):**
1. **Артисты:** Имя + количество альбомов. Клик → установить фильтр "Артист"
2. **Альбомы:** Обложка + название + артист. Клик → открыть треки альбома
3. **Треки:** Название + артист + альбом. Клик → воспроизвести в плеере

**Поведение:**
- Результаты поиска показываются ВМЕСТО обычной сетки альбомов
- При очистке поиска — возвращается обычный режим
- Фильтры продолжают работать параллельно с поиском

---

### 4.9. AudioPlayer

**Описание:** Нижний фиксированный плеер.

**Отображение:**
- Обложка трека (или альбома) — миниатюра
- Название трека
- Имя артиста — название альбома
- Кнопки управления: Play/Pause, Stop, Перемотка (seek bar)
- Время: текущее / общее
- Кнопка "Текст" — если у текущего трека есть lyrics
- Кнопка "Минус" — если у текущего трека есть `minus_url`
- Кнопка "Закрыть" (убрать плеер)

**Поведение:**
- Появляется при выборе трека
- Воспроизведение — через HTML5 Audio API
- Стриминг: используется `audio_url` (или `minus_url`, если включён режим минуса); браузер сам шлёт HTTP Range, бэкенд отвечает `206`
- Кнопка "Минус" в плеере переключает текущий трек на минусовку с начала; автопереход и Play снова включают оригинал
- Перемотка: клик или перетаскивание ползунка ставит `audio.currentTime`. Пока пользователь двигает ползунок и пока аудио не завершило seek, `timeupdate` не сбрасывает ползунок назад
- То же для Media Session `seekto` (экран блокировки / системные клавиши)
- Переключение треков: если пользователь кликает другой трек — плеер переключается
- Автопереход: после окончания трека играет следующий в альбоме; после последнего — снова первый
- Следующий трек предзагружается во второй `<audio>` (двойной буфер), чтобы переход работал при выключенном экране
- Media Session API: play/pause/next/prev, seekto и обложка на экране блокировки
- Кнопка "Закрыть" — останавливает воспроизведение и скрывает плеер

**Состояния:**
- Нет трека — плеер скрыт
- Трек загружается — спиннер
- Трек играет — кнопка Pause
- Трек на паузе — кнопка Play

---

### 4.10. LyricsDialog

**Описание:** Модальный просмотр текста песни.

**Отображение:**
- Заголовок: «Текст песни» + название трека
- Куплеты разделены пустой строкой, `whitespace-pre-wrap`, колонка `max-w-prose`
- На мобильных — нижняя шторка, на десктопе — центрированный диалог

**Поведение:**
- Открывается кнопкой «Текст» в списке треков или в плеере
- Закрытие: Escape, клик по фону, кнопка ✕
- Текст рендерится как обычный текст (без HTML)

---

## 5. Взаимодействие с API

### 5.1. Загрузка при старте
Шаг 1. Параллельно:
- GET /api/v1/genres/          (для карусели жанров)
- GET /api/v1/moods/           (для карусели настроений)
- GET /api/v1/artists/         (для карусели артистов — все артисты)
- GET /api/v1/albums/          (для карусели альбомов — все альбомы)
- GET /api/v1/tracks/          (до 20 треков каталога)

Шаг 2. Отобразить:
- Секция результатов: список треков (обложка слева), не больше 20
- Плеер: первый трек списка (на паузе, не воспроизводится автоматически)

---

### 5.2. Фасетная фильтрация

| Действие пользователя | Запрос |
|----------------------|--------|
| Выбор жанра | `GET /api/v1/artists/?genre_id=1` + `GET /api/v1/albums/?genre_id=1` + `GET /api/v1/tracks/?genre_id=1` |
| Выбор настроения | `GET /api/v1/artists/?mood_id=2` + `GET /api/v1/albums/?mood_id=2` + `GET /api/v1/tracks/?mood_id=2` |
| Выбор артиста | `GET /api/v1/albums/?artist_id=5` + `GET /api/v1/tracks/?artist_id=5` (+ жанр/настроение, если выбраны) |
| Выбор альбома | `GET /api/v1/albums/10/` (шапка) + `GET /api/v1/tracks/?album_id=10` (+ остальные фильтры) |
| Сброс жанра | `GET /api/v1/artists/?mood_id=2` + `GET /api/v1/tracks/?mood_id=2` (если настроение выбрано) |
| Сброс настроения | `GET /api/v1/artists/?genre_id=1` + `GET /api/v1/tracks/?genre_id=1` (если жанр выбран) |
| Сброс артиста | `GET /api/v1/albums/?genre_id=1&mood_id=2` + `GET /api/v1/tracks/?…` |
| Сброс всех | `GET /api/v1/artists/` + `GET /api/v1/albums/` + `GET /api/v1/tracks/` |

**Логика:**
- Карусель артистов обновляется при изменении ЖАНРА или НАСТРОЕНИЯ
- Карусель альбомов обновляется при изменении ЖАНРА, НАСТРОЕНИЯ или АРТИСТА
- Список треков внизу обновляется при ЛЮБОМ изменении фильтров и всегда пересекается по AND
- Все запросы — асинхронные, с debounce (300 мс) для избежания лишних вызовов

---

### 5.3. Поиск

| Действие | Запрос |
|----------|--------|
| Ввод в поиск (debounce 500 мс) | `GET /api/v1/search/?query=...` |

**Логика:**
- Поиск работает параллельно с фильтрами
- При активном поиске результаты поиска показываются вместо обычной сетки
- При очистке поиска — возвращается обычный режим

---

### 5.4. Воспроизведение

| Действие | Запрос |
|----------|--------|
| Выбор трека | `GET {audio_url}` (стриминг, Range для seek) |
| Перемотка ползунка | браузер: `Range: bytes=…` → `206 Partial Content` |
| Загрузка обложки | `GET {cover_url}` |

---

## 6. Состояния (Pinia)

### 6.1. `useCatalogStore`

**Хранит:**
- `genres: []` — список жанров (загружается при старте, не меняется)
- `moods: []` — список настроений (загружается при старте, не меняется)
- `artists: []` — текущий список артистов (обновляется при фильтрации)
- `albums: []` — текущий список альбомов (обновляется при фильтрации)
- `tracks: []` — текущий список треков в результатах (до 20, с учётом фильтров)
- `currentAlbum: null` — выбранный альбом (детали для шапки)
- `searchResults: null` — результаты поиска
- `initialAlbumLoaded: false` — флаг, что начальная загрузка каталога прошла
- `isInitialLoad: true` — флаг первичной загрузки
- `isTracksLoading: false`

**Действия:**
- `fetchGenres()`
- `fetchMoods()`
- `fetchArtists(genreId, moodId)`
- `fetchAlbums(artistId, genreId, moodId)`
- `fetchAlbumDetail(albumId)`
- `fetchTracks()` — `GET /tracks/` с текущими фильтрами, `page_size=20`
- `search(query)`
- `clearSearch()`
- `loadInitialContent()` — справочники, карусели и первые 20 треков; плеер с первым треком на паузе
---

### 6.2. `useFiltersStore`

**Хранит:**
- `selectedGenre: null`
- `selectedMood: null`
- `selectedArtist: null`
- `selectedAlbum: null`
- `searchQuery: ''`

**Геттеры:**
- `activeFilters` — массив выбранных фильтров (для чипов)
- `activeFiltersCount` — количество выбранных фильтров
- `hasActiveFilters` — есть ли хоть один выбранный фильтр
- `isDefaultState` — true, если ни один фильтр не выбран и поиск пуст

**Действия:**
- `setGenre(genre)` — устанавливает жанр, НЕ сбрасывает другие фильтры
- `setMood(mood)` — устанавливает настроение, НЕ сбрасывает другие фильтры
- `setArtist(artist)` — устанавливает артиста, НЕ сбрасывает другие фильтры
- `setAlbum(album)` — устанавливает альбом, НЕ сбрасывает другие фильтры
- `clearGenre()` — сбрасывает только жанр
- `clearMood()` — сбрасывает только настроение
- `clearArtist()` — сбрасывает только артиста
- `clearAlbum()` — сбрасывает только альбом
- `resetAll()` — сбрасывает все фильтры
- `setSearchQuery(query)` — устанавливает поисковый запрос

---

### 6.3. `usePlayerStore`

**Хранит:**
- `currentTrack: null` — текущий трек
- `isPlaying: false`
- `isMinus: false` — сейчас играет минусовка, а не оригинал
- `currentTime: 0`
- `duration: 0`
- `queue: []` — очередь воспроизведения (текущий список в результатах)
- `currentIndex: -1` — индекс текущего трека в очереди
- `lyricsTrack: null` — трек, чей текст открыт в диалоге

**Действия:**
- `playTrack(track, queue)` — начать воспроизведение оригинала трека с очередью
- `playMinus(track, queue)` — начать воспроизведение минусовки (только если есть `minus_url`)
- `togglePlay()` — play/pause
- `stop()` — остановить и скрыть плеер
- `seekTo(seconds)` — перемотка; значение ограничивается `[0, duration]`
- `next()` — следующий трек в очереди (с последнего — на первый)
- `prev()` — предыдущий трек в очереди (с первого — на последний)
- `peekNextTrack()` — следующий трек с учётом зацикливания альбома
- `openLyrics(track)` / `closeLyrics()` — диалог текста песни

Логика:

При isDefaultState === true — показывать до 20 треков каталога
При сбросе всех фильтров (resetAll()) — снова полный список (до 20)
---

## 7. UX-требования

### 7.1. Анимации
- Плавное появление каруселей (fade-in)
- Плавная прокрутка каруселей (scroll-behavior: smooth)
- Анимация появления плеера (slide-up)
- Спиннер при загрузке данных
- Плавное переключение режимов (сетка ↔ список треков)

### 7.2. Адаптивность

| Устройство | Плитка альбомов | Карусели |
|------------|-----------------|----------|
| Mobile (< 768px) | 2 колонки | Полноширинные, swipe |
| Tablet (768-1024px) | 3-4 колонки | Полноширинные |
| Desktop (> 1024px) | 5-6 колонок | Полноширинные |

### 7.3. Пустые состояния

| Ситуация | Сообщение |
|----------|-----------|
| Нет жанров | "Жанры не добавлены" |
| Нет настроений | "Настроения не добавлены" |
| Нет артистов (из-за фильтров) | "Нет артистов с такими параметрами" |
| Нет альбомов (из-за фильтров) | "Нет альбомов с такими параметрами" |
| Нет результатов | "Ничего не найдено. Измените параметры." |
| Поиск без результатов | "По запросу '...' ничего не найдено" |
| Нет выбранных фильтров и нет поиска | До 20 треков каталога по порядку |

### 7.4. Обработка ошибок
- Ошибка сети: Toast-уведомление "Не удалось загрузить данные"
- Ошибка API: Показывать сообщение из `error.message`
- Таймаут запроса: Повторная попытка (кнопка "Повторить")

### 7.5. Производительность
- Debounce для поиска: 500 мс
- Debounce для фильтров: 300 мс
- Кэширование жанров и настроений (не перезапрашивать)
- Ленивая загрузка обложек (lazy loading для изображений)

---

## 8. Технологии

| Технология | Назначение |
|------------|------------|
| Vue 3 | Фреймворк |
| Composition API | Стиль компонентов |
| Vite | Сборка |
| Pinia | State management |
| Axios | HTTP-клиент |
| Tailwind CSS | Стилизация |
| Quasar (опционально) | Готовые UI-компоненты (карусель, модалка) |
| HTML5 Audio API | Воспроизведение |

---

## 9. Роутинг (если потребуется)

Для MVP роутер не обязателен (одна страница). Но если понадобится:

| Путь | Компонент |
|------|-----------|
| `/` | Главная страница (каталог) |
| `/album/:id` | Детальный просмотр альбома (опционально) |
| `/admin` | Ссылка на Django Admin |